apiVersion: v1 
kind: Pod 
metadata:
  name: my-app
  labels:
    app: yogi-app
spec:
  containers:
    - name: http-container
      image: httpd
      ports:
        - name: http 
          protocol: TCP
          containerPort: 80
      env: 